import SwiftUI

// Phase 6에서 AppContainer(생성자 주입) + AppFlowCoordinator + 로그인 게이트로 교체.
// 지금은 generate 통과 + 빈 창 실행용 최소 스텁.
@main
struct GitSearchApp: App {
    var body: some Scene {
        WindowGroup {
            Text("GitSearch")
        }
    }
}
